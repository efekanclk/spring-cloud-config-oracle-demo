server:
  port: 8087

spring:
  application:
    name: myapp
  profiles:
    active: dev
  config:
    import: "configserver:http://localhost:8888/config"

management:
  endpoints:
    web:
      exposure:
        include: "*"
