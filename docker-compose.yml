version: '3.8'
services:
  configdb:
    image: container-registry.oracle.com/database/free:latest
    container_name: configdb
    ports:
      - "1527:1521"
    environment:
      ORACLE_PWD: "efekan12345"
    volumes:
      - ./oracle/grant_permissions.sql:/container-entrypoint-initdb.d/grant_permissions.sql